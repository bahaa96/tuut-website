{
  "version": 2,
  "outputDirectory": "build",
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
